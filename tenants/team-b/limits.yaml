apiVersion: v1
kind: LimitRange
metadata:
  name: team-b-limits
  namespace: team-b
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 512Mi
      defaultRequest:
        cpu: 200m
        memory: 256Mi
